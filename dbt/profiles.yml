moex_workshop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: etl
      password: etl
      port: 5432
      dbname: workshop
      schema: analytics
      threads: 2
      # Попадает в log_line_prefix Postgres как app=… (см. README, аудит SQL).
      connect_args:
        application_name: dbt_workshop
