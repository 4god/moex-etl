name: "moex_workshop_dbt"
version: "1.0.0"
config-version: 2

profile: "moex_workshop"

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  moex_workshop_dbt:
    +materialized: view
    staging:
      +materialized: view
    marts:
      +materialized: view
