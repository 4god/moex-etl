# DE-1234 example ad-hoc

Пример структуры одноразовой задачи от аналитиков:

- `context.md` — зачем задача нужна и как проверить результат;
- `query.sql` — одноразовый SQL;
- `rollback.sql` — опционально откат.

Эти файлы не должны вызываться регулярными DAG-ами.
