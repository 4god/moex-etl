# Совместимо с apache/airflow:2.10.4-python3.11 — ставим с constraints Airflow (см. Dockerfile).
# dbt-postgres ставится отдельным pip install, иначе pip падает с ResolutionTooDeep.
apache-airflow-providers-postgres==5.14.0
apache-airflow-providers-http==4.13.3
kafka-python==2.0.2
requests==2.32.3
PyYAML==6.0.2
